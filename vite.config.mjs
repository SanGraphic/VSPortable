export default {
  root: '.webpack/renderer',
  server: {
    port: 3000,
    strictPort: true
  },
  resolve: {
    alias: {
      // We might need aliases later for the src/ modular code
    }
  }
};
